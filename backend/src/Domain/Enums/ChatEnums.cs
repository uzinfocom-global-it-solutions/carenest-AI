namespace Backend.Domain.Enums;

public enum ChatTypeEnum { Family, Private, Child }

public enum SenderTypeEnum { User, Ai, System }

public enum MessageTypeEnum { Message, Question, Recommendation, Reminder, Alert, Proposal, Confirmation }

public enum ProposalStatusEnum { Pending, Confirmed, Rejected, Failed }
